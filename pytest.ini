[pytest]
# The host tools and their tests all live at the repository root. Naming them keeps collection
# to this directory, so a test_*.py inside a submodule, a build tree or an ESP-IDF component
# never ends up in a run that installs nothing but requirements-dev.txt.
testpaths = .
norecursedirs = .* build components docs examples main owl __pycache__
